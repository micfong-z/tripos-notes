The geometric definition uses $cos theta$, so it requires $-1 <= cos theta <= 1$. Cauchy-Schwarz provides exactly this bound:
$ abs(cos theta) = (abs(bold(a) dot bold(b)))/(abs(bold(a)) abs(bold(b))) <= 1 $
whenever both vectors are non-zero.

If $X ~ U[a, b]$, then for $x in [a, b]$,
$
  PP(X <= x) = integral_(-oo)^x f(t) dif t = (x - a)/(b - a),
$
while $PP(X <= x) = 0$ for $x < a$ and $PP(X <= x) = 1$ for $x > b$. In particular, for $U[0, 1]$ we have $PP(X <= x) = x$ for all $x in [0, 1]$.

Conversely, the gradient vector $bold(nabla) f$ can be defined geometrically the other way round, as the vector such that
$
  (dif f)/(dif s) = bold(hat(s)) dot bold(nabla) f quad forall bold(hat(s)).
$
Knowing the rate of change of $f$ in every unit direction therefore determines $bold(nabla) f$ itself.

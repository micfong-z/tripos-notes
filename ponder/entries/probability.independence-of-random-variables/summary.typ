Discrete random variables $X_1, ..., X_n$ with values in $S_1, ..., S_n$ are independent when
$
  PP(X_1 = x_1, ..., X_n = x_n) = PP(X_1 = x_1) dot ... dot PP(X_n = x_n)
$
for any $x_1 in S_1, ..., x_n in S_n$: joint masses factorise into products of marginal masses.

Given a set of size $n$, there are $n(n-1)dots.c(n-k+1)$ ways to pick $k$ elements, in order, one by one. But each subset of size $k$ is picked in $k!$ ways by this method. Hence the number of subsets of size $k$ in ${1, 2, ..., n}$ is
$ binom(n, k) = (n(n-1)dots.c(n-k+1))/k! = n!/(k!(n-k)!). $

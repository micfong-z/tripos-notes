There are "arithmetic systems" permitting $+$ and $times$ in which factorisation is not unique. Consider $ZZ[sqrt(-3)]$, the set of all numbers of the form $a + b sqrt(-3)$ where $a, b in ZZ$, with addition and multiplication defined as usual. In this system one can define what it means to be a "prime", and $1 plus.minus sqrt(-3)$ happen to be primes in this sense. Yet $4 = 2 times 2$, so factorisation is not unique: uniqueness of prime factorisation genuinely relies on the arithmetic of the natural numbers.

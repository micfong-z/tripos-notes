$
  PP(A) & = PP(A inter (union.big_n B_n)) \
        & = PP(union.big_n (A inter B_n)) \
        & = sum_n PP(A inter B_n) \
        & = sum_n PP(A mid(|) B_n) PP(B_n),
$
since the $B_n$ are disjoint.

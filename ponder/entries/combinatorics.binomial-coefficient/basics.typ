By definition, $binom(n, 0) = 1$, $binom(n, n) = 1$, and $binom(n, 1) = n$ for $n > 0$. Grouping subsets by their size counts every subset of an $n$-element set exactly once:
$ binom(n, 0) + binom(n, 1) + binom(n, 2) + ... + binom(n, n) = 2^n. $

Squaring the deviation and applying Markov's inequality to the non-negative random variable $(X - EE[X])^2$,
$
  PP(abs(X-EE[X]) >= a) & = PP(abs(X - EE[X])^2 >= a^2) \
                        & <= (EE[(X - EE[X])^2]) / (a^2) \
                        & = ("Var"(X)) / (a^2).
$

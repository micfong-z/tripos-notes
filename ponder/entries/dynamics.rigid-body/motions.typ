The only motions a rigid body can undergo are translations of the centre of mass, and rotations. A rotation is described by an angular velocity vector $bold(omega) = omega bold(hat(n))$, where $bold(hat(n))$ points along the axis of rotation and $omega = abs(bold(omega)) = dot(theta)$ is the angular speed of rotation, with direction determined by the right hand rule:
$
  bold(dot(x)) = bold(omega) times bold(x),
$
where $bold(x)$ is the position relative to some (any) point on the axis of rotation, since there are infinitely many parallel axes an object can rotate about.

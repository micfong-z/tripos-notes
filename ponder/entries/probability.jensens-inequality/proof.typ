Let $m = EE[X]$. By the supporting-line property of convex functions, there exist $a, b in RR$ with
$
  f(x) & >= a x + b quad "for all" x in RR, \
  f(m) & = a m + b.
$
Then $f(X) >= a X + b$ pointwise, and taking expectations,
$
  EE[f(X)] >= a EE[X] + b = a m + b = f(m) = f(EE[X]).
$

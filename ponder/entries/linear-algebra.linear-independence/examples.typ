1. ${vec(1,0), vec(0,1), vec(0,2)}$ in $RR^2$ is linearly dependent, since $2 vec(0,1) = vec(0,2)$.

2. ${vec(1,0), vec(0,1)}$ in $RR^2$ is linearly independent.

3. Any set containing $bold(0)$ is linearly dependent.

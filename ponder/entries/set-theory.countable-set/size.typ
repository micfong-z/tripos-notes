A set is countable precisely when it is _at most as big as_ $NN$. Accordingly:
- an injection $X -> NN$ makes $X$ countable;
- a surjection $NN -> X$, or $X = emptyset$, likewise makes $X$ countable.

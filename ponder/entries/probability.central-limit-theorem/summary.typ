For i.i.d. $X_i$ with mean $mu$ and variance $sigma^2 < oo$, the standardised sums converge in #ponder("probability.convergence-in-distribution")[distribution] to a standard normal:
$
  (S_n - n mu)/(sigma sqrt(n)) stretch(->)^((d))_(n->oo) N(0, 1).
$

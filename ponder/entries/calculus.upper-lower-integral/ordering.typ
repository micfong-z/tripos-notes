Always $L(f, cal(P)) <= U(f, cal(P'))$ for any two partitions, so $I_*(f) <= I^*(f)$. Hence $f$ is Riemann integrable iff $I_* (f) >= I^* (f)$.

With the convention $integral_a^b f dif x = -integral_b^a f dif x$ built into the setup of the sums, the definition extends to oriented intervals.

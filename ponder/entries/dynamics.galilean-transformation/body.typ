Under a Galilean transformation,
$
  bold(dot.double(x)) = bold(0) <=> bold(dot.double(x')) = bold(0).
$
Indeed, differentiating $bold(x)' = matbold(R) bold(x) + bold(k) + bold(w) t$ twice gives $bold(dot.double(x')) = matbold(R) bold(dot.double(x))$, and a rotation or reflection is invertible, so one acceleration vanishes exactly when the other does.

A frame related to an inertial frame by a Galilean transformation is therefore also an inertial frame. Acceleration is not relative: it has the same magnitude in all inertial frames.

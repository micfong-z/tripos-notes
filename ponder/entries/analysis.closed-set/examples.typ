- $[0, 1]$ is closed and so is $[0, oo)$.

- $(0, 1)$, $(0, 1]$, and $[0, 1)$ are not closed: for instance $x_n = 1/n$ lies in each of them and converges to $0$, which is outside all three.

- $(0, oo)$ is not closed.

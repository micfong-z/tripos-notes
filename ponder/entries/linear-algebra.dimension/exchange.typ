In a vector space $V$ of dimension $n$:

1. If $Y = {bold(w_1), ..., bold(w_m)}$ spans $V$ with $m > n$, removing vectors from $Y$ yields a basis.

2. If $Z = {bold(u_1), ..., bold(u_k)}$ is a linearly independent set with $k < n$, adding vectors to $Z$ yields a basis.

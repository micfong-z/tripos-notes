If the MGF is defined for an interval of values of $theta$, then
$
  m^((r))(0) = EE[X^r],
$
so every moment of $X$ is read off from a derivative of $m$ at $0$. In particular $m'(0) = EE[X]$ and $m''(0) = EE[X^2]$.

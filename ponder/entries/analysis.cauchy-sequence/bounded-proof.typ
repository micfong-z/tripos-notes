Take $epsilon = 1$. There is an $N$ with $abs(x_n - x_N) < 1$ for all $n >= N$, hence $abs(x_n) < 1 + abs(x_N)$ there. Since $x_N$ is a fixed number, the whole sequence is bounded by
$
  max {abs(x_1), abs(x_2), ..., abs(x_(N-1)), 1 + abs(x_N)}.
$

Conditioning on $Y = y$ defines a conditional expectation by averaging against the conditional density: $EE[X mid(|) Y] = g(Y)$ where
$
  g(y) = integral_(-oo)^(oo) x f_(X mid(|) Y) (x mid(|) y) dif x.
$
As in the discrete case, $EE[X mid(|) Y]$ is a random variable that is a function of $Y$.

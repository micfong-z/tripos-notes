$X = (X_1, ..., X_n): Omega -> RR^n$ is a random variable in $RR^n$ when
$
  {X_1 <= x_1, ..., X_n <= x_n} = {omega in Omega: X_i (omega) <= x_i "for all" i} in cal(F)
$
for all $x_1, ..., x_n in RR$, equivalently when every coordinate $X_i$ is a real random variable.

If $a_n -> l$, then some $N in NN$ satisfies $abs(a_n - l) < 1$ for all $n >= N$. Hence for all $n in NN$,
$ abs(a_n) <= max {abs(a_1), abs(a_2), ..., abs(a_(N-1)), abs(l) + 1}, $
so the sequence is bounded. The converse fails: $a_n = (-1)^n$ is bounded but diverges.
